package com.renshuo.cloud.${domain.module}.dao;

import org.apache.ibatis.annotations.Mapper;

/**
* @description: ${domain.comments}接口，在xml中引用
* @author: renshuo
* @date: ${date}
*/
@Mapper
public interface ${domain.name}Mapper{


}